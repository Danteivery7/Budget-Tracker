{
  "name": "budget-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check engine.js && node --check netlify/functions/auth.mjs && node --check netlify/functions/budget.mjs && node --check netlify/lib/auth.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.13"
  }
}
